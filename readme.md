\# Python CSV to PostgreSQL ETL

Successfully completed on Monday.



\## Features:

\- Connected Python to PostgreSQL 18.

\- Cleaned data using Pandas (dropped NaNs, stripped strings).

\- Uploaded clean data to the database using SQLAlchemy.
## 📈 Visualization
The project now includes `visualize_weather.py` which:
1. Connects to the PostgreSQL database.
2. Queries historical weather data.
3. Generates a line chart (`weather_plot.png`) showing temperature trends over time.
